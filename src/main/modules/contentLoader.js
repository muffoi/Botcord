const { loadGuilds, selectGuild, loadChannels, selectChannel, loadChat } = require("./contentLoader/guilds");

module.exports = {
    loadGuilds,
    selectGuild,
    loadChannels,
    selectChannel,
    loadChat
}